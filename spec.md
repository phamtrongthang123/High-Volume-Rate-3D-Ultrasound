I want to run the checkpoint trained from training/ folder as diffusion prior for the current pipeline here instead of the default version here. But i dont want to rewrite the script here, so i need a separate folder to run it.

**Constraints:**
- Use only the local GPU. Do NOT use SLURM, multi-GPU, or distributed training. Single GPU only.